<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Register - Isaac's Supermarket</title>

    <style>
        body { background: #f6fbf6; font-family: 'Sora','Inter','Segoe UI',sans-serif; min-height: 100vh; }
        .auth-navbar { background: #000; padding: 1rem; margin-bottom: 2rem; }
        .auth-navbar a { color: white; text-decoration: none; font-weight: 700; font-size: 1.25rem; display: inline-flex; align-items: center; gap: 0.5rem; }
        .auth-navbar a:hover { opacity: 0.8; }
        .auth-container { display: flex; align-items: center; justify-content: center; min-height: calc(100vh - 80px); padding: 2rem 1rem; }
        .auth-box { background: #fff; border-radius: 20px; padding: 40px; width: 100%; max-width: 460px; box-shadow: 0 12px 35px rgba(0,0,0,0.08); border: 1px solid #e8f1eb; }
        .auth-header { text-align: center; margin-bottom: 32px; }
        .auth-header h1 { font-size: 28px; font-weight: 700; color: #0f5132; margin-bottom: 8px; }
        .auth-header p { color: #666; font-size: 14px; margin: 0; }
        .form-control { border-radius: 12px; padding: 12px 16px; height: 48px; border: 1px solid #dce7dd; font-size: 15px; }
        .form-control:focus { border-color: #0f5132; box-shadow: 0 0 0 3px rgba(15,81,50,0.1); }
        label { font-weight: 600; color: #2d4737; font-size: 14px; margin-bottom: 8px; }
        .btn-auth { border-radius: 12px; padding: 12px 18px; font-weight: 700; height: 48px; font-size: 16px; }
        .btn-success { background: #0f5132; border: none; }
        .btn-success:hover { background: #0d4029; }
        .auth-link { text-align: center; margin-top: 18px; font-size: 14px; color: #666; }
        .auth-link a { color: #0f5132; text-decoration: none; font-weight: 700; }
        .auth-link a:hover { text-decoration: underline; }
        .alert { border-radius: 12px; border: none; }
        @media (max-width: 575px) { .auth-box { padding: 24px; } .auth-header h1 { font-size: 24px; } }
    </style>
</head>
<body>

    <!-- NAVBAR -->
    <div class="auth-navbar">
        <a href="/"><i class="bi bi-shop"></i> Isaac's Supermarket</a>
    </div>

    <!-- REGISTER BOX -->
    <div class="auth-container">
        <div class="auth-box">
            <div class="auth-header">
                <h1><i class="bi bi-shop"></i> Isaac's Supermarket</h1>
                <p>Create your account</p>
            </div>

            <% if (messages && messages.length > 0) { %>
                <div class="alert alert-danger mb-3">
                    <% messages.forEach(message => { %>
                        <p class="mb-1"><%= message %></p>
                    <% }); %>
                </div>
            <% } %>

            <form action="/register" method="POST">
                <div class="mb-3">
                    <label for="username">Username</label>
                    <input id="username" type="text" name="username" class="form-control" required value="<%= formData?.username || '' %>">
                </div>

                <div class="mb-3">
                    <label for="email">Email</label>
                    <input id="email" type="email" name="email" class="form-control" required value="<%= formData?.email || '' %>">
                </div>

                <div class="mb-3">
                    <label for="password">Password</label>
                    <input id="password" type="password" name="password" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label for="confirmPassword">Confirm Password</label>
                    <input id="confirmPassword" type="password" name="confirmPassword" class="form-control" required>
                    <small class="text-muted">Must match your password</small>
                </div>

                <div class="mb-3">
                    <label for="address">Address</label>
                    <input id="address" type="text" name="address" class="form-control" required value="<%= formData?.address || '' %>">
                </div>

                <div class="mb-4">
                    <label for="contact">Contact Number (8 digits)</label>
                    <input id="contact" type="text" name="contact" class="form-control" pattern="\d{8}" placeholder="e.g., 96543210" maxlength="8" required value="<%= formData?.contact || '' %>">
                    <small class="text-muted">Singapore phone number without country code (+65)</small>
                </div>

                <button type="submit" class="btn btn-success btn-auth w-100"><i class="bi bi-person-plus"></i> Create Account</button>
            </form>

            <div class="auth-link">Already have an account? <a href="/login">Log in here</a></div>
        </div>
    </div>

</body>
</html>
