<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="/css/style.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Edit Product - Isaac's Supermarket</title>
</head>
<body>
  <%- include('partials/adminnavbar') %>
  <div class="container mt-4">
    <h2 class="text-center mb-4">Edit Product</h2>
          <% if (product) { %> 
            <form action="/updateProduct/<%= product.id %>" method="POST" enctype="multipart/form-data">

                <!-- 🔥 FIXED: name="productName" -->
                <label for="productName">Product Name:</label><br>
                <input type="text" id="productName" name="productName" value="<%= product.productName %>" required><br><br>

                <label for="quantity">Quantity:</label><br>
                <input type="number" id="quantity" name="quantity" min="0" step="1" value="<%= product.quantity %>" required><br><br>

                <label for="price">Price:</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" required><br><br>

                <!-- CATEGORY DROPDOWN -->
                <label for="category">Category:</label><br>
                <select name="categoryId" class="form-select text-center" style="width: 200px; margin: 0 auto;" required>
                  <% categories.forEach(c => { %>
                    <option value="<%= c.id %>" <%= product.categoryId == c.id ? "selected" : "" %>>
                      <%= c.name %>
                    </option>
                  <% }) %>
                </select>
                <br>

                <label>Current Image:</label><br>
                <input type="text" name="existingImage" value="<%= product.image %>" readonly><br>
                <img src="/images/<%= product.image %>" width="20%"><br><br>
                
                <label>New Image:</label><br>
                <input type="file" id="image" name="image" accept="image/*"><br><br>

                <button type="submit" class="btn btn-primary">Update Product</button>
            </form>
          <% } else { %>
              <p>No product found.</p>
          <% } %>

          <a href="/inventory" class="btn btn-secondary mt-3">Back</a>
        </div>

    </div>
</body>
</html>
