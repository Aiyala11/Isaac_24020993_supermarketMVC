<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <title>Login - Isaac's Supermarket</title>

  <style>
    body { background: #f6fbf6; font-family: 'Sora','Inter','Segoe UI',sans-serif; min-height: 100vh; }
    .auth-navbar { background: #000; padding: 1rem; margin-bottom: 2rem; }
    .auth-navbar a { color: white; text-decoration: none; font-weight: 700; font-size: 1.25rem; display: inline-flex; align-items: center; gap: 0.5rem; }
    .auth-navbar a:hover { opacity: 0.8; }
    .auth-container { display: flex; align-items: center; justify-content: center; min-height: calc(100vh - 80px); padding: 2rem 1rem; }
    .auth-box { background: #fff; border-radius: 20px; padding: 40px; width: 100%; max-width: 420px; box-shadow: 0 12px 35px rgba(0,0,0,0.08); border: 1px solid #e8f1eb; }
    .auth-header { text-align: center; margin-bottom: 32px; }
    .auth-header h1 { font-size: 28px; font-weight: 700; color: #0f5132; margin-bottom: 8px; }
    .auth-header p { color: #666; font-size: 14px; margin: 0; }
    .form-control { border-radius: 12px; padding: 12px 16px; height: 48px; border: 1px solid #dce7dd; font-size: 15px; }
    .form-control:focus { border-color: #0f5132; box-shadow: 0 0 0 3px rgba(15,81,50,0.1); }
    label { font-weight: 600; color: #2d4737; font-size: 14px; margin-bottom: 8px; }
    .btn-auth { border-radius: 12px; padding: 12px 18px; font-weight: 700; height: 48px; font-size: 16px; }
    .btn-success { background: #0f5132; border: none; }
    .btn-success:hover { background: #0d4029; }
    .auth-link { text-align: center; margin-top: 18px; font-size: 14px; color: #666; }
    .auth-link a { color: #0f5132; text-decoration: none; font-weight: 700; }
    .auth-link a:hover { text-decoration: underline; }
    .alert { border-radius: 12px; border: none; }
    @media (max-width: 575px) { .auth-box { padding: 24px; } .auth-header h1 { font-size: 24px; } }
  </style>
</head>

<body>

  <!-- NAVBAR -->
  <div class="auth-navbar">
    <a href="/"><i class="bi bi-shop"></i> Isaac's Supermarket</a>
  </div>

  <!-- LOGIN BOX -->
  <div class="auth-container">
    <div class="auth-box">
      <div class="auth-header">
        <h1><i class="bi bi-shop"></i> Isaac's Supermarket</h1>
        <p>Welcome back</p>
      </div>

      <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show mb-3" role="alert">
          <i class="bi bi-exclamation-circle"></i>
          <% errors.forEach(function(error) { %>
            <div><%= error %></div>
          <% }); %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>

      <% if (messages && messages.length > 0) { %>
        <div class="alert alert-success mb-3">
          <% messages.forEach(function(message) { %>
            <p class="mb-1"><%= message %></p>
          <% }); %>
        </div>
      <% } %>

    <form action="/login" method="POST">


      <div class="mb-3">
        <label for="email">Email</label>
        <input id="email" type="email" name="email" class="form-control" autocomplete="email" required value="<%= formData?.email || '' %>">
      </div>

      <div class="mb-4">
        <label for="password">Password</label>
        <input id="password" type="password" name="password" class="form-control" autocomplete="current-password" required value="<%= formData?.password || '' %>">
      </div>

      <button type="submit" class="btn btn-success btn-auth w-100"><i class="bi bi-box-arrow-in-right"></i> Login</button>

    </form>

    <div class="auth-link">Don't have an account? <a href="/register">Sign up here</a></div>
    </div>
  </div>

</body>
</html>
